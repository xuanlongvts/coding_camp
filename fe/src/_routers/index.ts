const admin = '/admin';

const LinkRouters = {
    presentation: '/presentation',
    admin: admin,
    adminDashboard: `${admin}/dashboard`,
    adminProductActions: `${admin}/product-actions`,
    mintNft: `${admin}/mint-nft`,
    setting: `${admin}/setting`,
};

export default LinkRouters;
